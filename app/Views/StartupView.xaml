<UserControl x:Class="PcOptimizer.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>
    <Style x:Key="RiskMini" TargetType="TextBlock">
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
      <Style.Triggers>
        <DataTrigger Binding="{Binding Risk}" Value="MEDIO">
          <Setter Property="Foreground" Value="{StaticResource AmberBrush}" />
        </DataTrigger>
        <DataTrigger Binding="{Binding Risk}" Value="ALTO">
          <Setter Property="Foreground" Value="{StaticResource OrangeBrush}" />
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </UserControl.Resources>

  <Grid Margin="24,20,24,18">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,14">
      <StackPanel>
        <TextBlock Style="{StaticResource H1}" Text="Apps al inicio" />
        <TextBlock Style="{StaticResource Muted}" Margin="0,4,0,0"
                   Text="Lo que arranca con Windows. Deshabilitar acelera el booteo y es 100% reversible." />
      </StackPanel>
      <Button x:Name="RescanButton" Style="{StaticResource GhostButton}" Content="Recargar" HorizontalAlignment="Right" VerticalAlignment="Top" Click="Rescan_Click" />
    </Grid>

    <Border Grid.Row="1" Background="#FF0E141B" CornerRadius="8">
      <DataGrid x:Name="Grid" Margin="2">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Programa" Binding="{Binding Name}" Width="1.4*" IsReadOnly="True" />
          <DataGridTextColumn Header="Comando / ubicación del archivo" Binding="{Binding Command}" Width="2*" IsReadOnly="True">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
              </Style>
            </DataGridTextColumn.ElementStyle>
          </DataGridTextColumn>
          <DataGridTextColumn Header="Origen" Binding="{Binding LocationText}" Width="180" IsReadOnly="True">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" /></Style>
            </DataGridTextColumn.ElementStyle>
          </DataGridTextColumn>
          <DataGridTemplateColumn Header="Riesgo" Width="78">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Risk}" Style="{StaticResource RiskMini}" VerticalAlignment="Center" ToolTip="{Binding RiskReason}" />
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTemplateColumn Header="Activo al inicio" Width="120">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <CheckBox IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          Content="{Binding StateText}" Foreground="{StaticResource TextPrimaryBrush}"
                          VerticalAlignment="Center" Click="Toggle_Click" />
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </Border>

    <TextBlock Grid.Row="2" x:Name="Status" Margin="2,14,0,0" Foreground="{StaticResource TextMutedBrush}"
               Text="Leyendo apps de inicio..." />
  </Grid>
</UserControl>
